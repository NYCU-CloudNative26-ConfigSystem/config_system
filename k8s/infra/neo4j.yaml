apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: neo4j-regional-pd
provisioner: pd.csi.storage.gke.io
parameters:
  type: pd-standard
volumeBindingMode: WaitForFirstConsumer
allowVolumeExpansion: true
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: neo4j
  namespace: ${K8S_NAMESPACE}
spec:
  serviceName: neo4j
  replicas: 1
  selector:
    matchLabels: {app: neo4j}
  template:
    metadata:
      labels: {app: neo4j}
    spec:
      enableServiceLinks: false
      containers:
        - name: neo4j
          image: neo4j:5-community
          ports:
            - {containerPort: 7474, name: http}
            - {containerPort: 7687, name: bolt}
            - {containerPort: 2004, name: prometheus}
          env:
            - name: NEO4J_AUTH
              valueFrom:
                secretKeyRef: {name: shared-secrets, key: NEO4J_AUTH}
            - name: NEO4J_server_config_strict__validation_enabled
              value: "false"
            - name: NEO4J_server_metrics_prometheus_enabled
              value: "true"
            - name: NEO4J_server_metrics_prometheus_endpoint
              value: "0.0.0.0:2004"
          volumeMounts:
            - {name: data, mountPath: /data}
          readinessProbe:
            httpGet: {path: /, port: 7474}
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 10
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
  volumeClaimTemplates:
    - metadata: {name: data}
      spec:
        accessModes: [ReadWriteOnce]
        storageClassName: neo4j-regional-pd
        resources:
          requests: {storage: 5Gi}
---
apiVersion: v1
kind: Service
metadata:
  name: neo4j
  namespace: ${K8S_NAMESPACE}
spec:
  selector: {app: neo4j}
  ports:
    - {name: http, port: 7474, targetPort: 7474}
    - {name: bolt, port: 7687, targetPort: 7687}
    - {name: prometheus, port: 2004, targetPort: 2004}
