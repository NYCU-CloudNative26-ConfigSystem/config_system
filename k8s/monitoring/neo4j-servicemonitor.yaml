apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: neo4j
  namespace: ${K8S_NAMESPACE}
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: neo4j
  endpoints:
    - port: prometheus
      interval: 30s
      path: /metrics
